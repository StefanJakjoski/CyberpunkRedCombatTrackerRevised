version: '3.9'

services:
  backend:
    build: .
    container_name: tracker-backend
    ports:
      - "5000:5000"
    environment:
      - PORT=5000
      - MONGO_URI=mongodb+srv://mongodbacc1988_db_user:n3FmuML0iJe7fKhB@tracker.aui9qod.mongodb.net/
      - MONGO_PASS=n3FmuML0iJe7fKhB
      - DB_NAME=Tracker
      - JWT_SECRET=W4k3Up54mur41W360t4C1tyT8urn
    depends_on:
      - mongo
    restart: unless-stopped

  mongo:
    image: mongo:7
    container_name: gonk-mongo
    ports:
      - "27017:27017"
    volumes:
      - mongo_data:/data/db
    restart: unless-stopped

volumes:
  mongo_data:
